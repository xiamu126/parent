server:
  port: 8081
spring:
  main:
    banner-mode: "off"
  session:
    timeout: 30m
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    cache: true
  profiles:
    include:
    active: prod-test
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true