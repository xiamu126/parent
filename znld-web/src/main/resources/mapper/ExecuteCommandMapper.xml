<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sybd.znld.mapper.ExecuteCommandMapper">
    <select id="getByCommand" parameterType="string" resultType="com.sybd.znld.model.ExecuteCommandEntity">
        select * from execute_command where `value` = #{_parameter}
    </select>
</mapper>